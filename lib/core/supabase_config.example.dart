/// Copy this file to `supabase_config.dart` (same folder) and fill in your
/// Supabase project's URL and anon key. `supabase_config.dart` is gitignored
/// so your keys never get committed.
class SupabaseConfig {
  static const String url = 'https://YOUR_PROJECT_REF.supabase.co';
  static const String anonKey = 'YOUR_ANON_KEY';
}
